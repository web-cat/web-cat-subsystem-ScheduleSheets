{
    attributes = (
        {
            columnName = id;
            externalType = INT;
            name = id;
            valueClassName = NSNumber;
            valueType = l;
        },
        {
            columnName = id1;
            externalType = INT;
            name = id1;
            valueClassName = NSNumber;
            valueType = l;
        }
    );
    attributesUsedForLocking = (id, id1);
    className = EOGenericRecord;
    externalName = EntryStudentNewResponsible;
    fetchSpecificationDictionary = {};
    name = EntryStudentNewResponsible;
    primaryKeyAttributes = (id, id1);
    relationships = (
        {
            destination = SheetEntry;
            isMandatory = Y;
            isToMany = N;
            joinSemantic = EOInnerJoin;
            joins = ({destinationAttribute = id; sourceAttribute = id; });
            name = entry;
        },
        {
            destination = User;
            isMandatory = Y;
            isToMany = N;
            joinSemantic = EOInnerJoin;
            joins = ({destinationAttribute = id; sourceAttribute = id1; });
            name = user;
        }
    );
}
